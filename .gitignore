node_modules/
.next/
out/
build/
dist/

# 环境变量：只提交 .env.example
.env
.env.local
.env*.local

# 审计日志（append-only JSONL，本地跑出来的，不进仓库）
.audit/

# SQLite 库文件：首次启动自动创建并写入种子数据，不需要进仓库。
# 想让演示数据「回到刚 clone 的样子」，删掉这个目录再启动即可。
#
# 前导斜杠不能省：写成 `data/` 会匹配任意层级的同名目录，
# 把 core/data/ 这个源码目录一起吞掉 —— 本地照样能跑（文件在磁盘上），
# 但 clone 下来会少一整个数据层，编译直接挂。这类「只在别人机器上炸」的
# 忽略规则，是最不值得踩的一种坑。
/data/

# 评测产物
eval-runs/*.json
eval-runs/*.md
!eval-runs/.gitkeep

*.log
npm-debug.log*
.DS_Store
Thumbs.db

# TS
*.tsbuildinfo
next-env.d.ts
